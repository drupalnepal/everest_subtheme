1. Install bundler.
$ sudo gem install bundler

2. Install gems.
$ bundle install

3. Install Grunt's command line interface (CLI) globally.
$ sudo npm install -g grunt-cli

4. Install project dependencies.
$ sudo npm install

5. Run Grunt
------------
(Development)
$ grunt

(Production)
$ grunt dist

(Delete files and folders created by grunt)
$ grunt clean
